import {NextResponse} from 'next/server';import crypto from 'crypto';
export async function GET(){const id=process.env.FORTYTWO_CLIENT_ID,redirect=process.env.FORTYTWO_REDIRECT_URI;if(!id||!redirect)return NextResponse.json({error:'42 OAuth is not configured'},{status:500});const state=crypto.randomBytes(32).toString('hex');const u=new URL('https://api.intra.42.fr/oauth/authorize');u.searchParams.set('client_id',id);u.searchParams.set('redirect_uri',redirect);u.searchParams.set('response_type','code');u.searchParams.set('scope','public');u.searchParams.set('state',state);const r=NextResponse.redirect(u);r.cookies.set('oauth_state',state,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:600});return r}
